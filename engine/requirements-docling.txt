# Docling and all it brings - PyTorch, pandas 2, transformers - installed by cell 1 into a folder of its
# own, never into the runtime's packages: Docling runs in a process of its own, so the runtime keeps its
# pandas, numpy and pyarrow as they are.
# Installed from PyPI. PyTorch from PyPI brings its GPU libraries - several GB, which a cluster without a GPU
# leaves unused.
docling>=2.129
