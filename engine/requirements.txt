# Everything must exist on the internal JFrog index (plan 2.15). Required first.
PyYAML>=6.0
openpyxl>=3.1
python-docx>=1.1
numpy>=1.24
scipy>=1.10
sympy>=1.12
rdata>=1.0
# Optional: each missing one degrades a named capability into a status and a flagged item.
pdfplumber>=0.10
pypdf>=4.0
# pyreadr   (optional second reader of parameter data; compiled wheel)
